<div class="full-width-image">
  {% if component
    .src
    .endsWith('.mov')or component
    .src
    .endsWith('.mp4') %}
    {# <video autoplay playsinline class="imgFullW" src="{{ component #}
    {# .src }}" alt="{{ image.alt }}" controls></video> #}
    <video playsinline class="imgFullW" src="{{ component
    .src }}" alt="{{ image.alt }}" controls></video>
  {% else %}
    <img class="imgFullW" src="{{ component.src }}" alt="{{ fullWidthImage.alt }}">
  {% endif %}
</div>